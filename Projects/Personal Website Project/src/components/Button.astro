---
export interface Props {
  text: string;
  href?: string;
}
const { text, href } = Astro.props;
---

<a href={href || "#"} class="inline-block px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition">
  {text}
</a>
