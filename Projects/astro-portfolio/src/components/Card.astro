---
const { title, description, link, image } = Astro.props;
---

<div class="bg-white dark:bg-gray-800 shadow-card rounded-xl overflow-hidden hover:shadow-lg transition flex flex-col">
  {image && (
  <img
    src={image}
    alt={title}
    class="w-full aspect-[3/4] object-contain bg-white dark:bg-gray-800"
  />

  )}
  <div class="p-6 flex flex-col justify-between flex-1">
    <h2 class="text-xl font-semibold mb-2">{title}</h2>
    <p class="text-gray-600 dark:text-gray-300 mb-4">{description}</p>
    <a href={link} class="inline-block text-indigo-600 hover:underline font-medium">
      Learn More →
    </a>
  </div>
</div>
